import styled from "styled-components";

export const FiltersContentContainer = styled.div`
  display: flex;
  flex-direction: column;
  width: var(--agents-filters-content-container-width);
  height: auto;
  gap: var(--agents-filters-content-container-gap);
  padding: var(--agents-filters-content-container-padding);
`;
